import { makeStyles } from "@material-ui/core/styles";

const useStyles = makeStyles(() => ({
  mainCard:{
    backgroundColor: "#fff",
    color: "#000",
    opacity: .7
  }
}));

export default useStyles;